env: dev
dubbo:
  application:
    qosEnable: false
  protocol:
    name: dubbo
    port: 18083
  registry:
    address: zookeeper://${zookeeperServerLists}
    file: ${user.home}/dubbo-cache/${spring.application.name}/dubbo.cache
    id: zk
  scan:
    basePackages: io.github.harvies.pluto
  metadata-report:
    address: zookeeper://${zookeeperServerLists}
  consumer:
    check: false
spring:
  application:
    name: pluto