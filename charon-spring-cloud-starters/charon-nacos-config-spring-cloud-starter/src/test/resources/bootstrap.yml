server:
  port: 8080
spring:
  application:
    name: nacos-config
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: ${charon.nacos.server-addr}
        namespace: ${spring.profiles.active}
        group: 1.0.0
logging:
  level:
    root: info