server:
  port: 8080
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        server-addr: ${charon.nacos.server-addr}
        ## bug 不配置会有问题
        prefix: ${spring.application.name}
logging:
  level:
    root: info