server:
  port: 8080
spring:
  application:
    name: nacos-discovery
  cloud:
    nacos:
      discovery:
        server-addr: ${charon.nacos.server-addr}
        metadata:
          request_tag: harvies
logging:
  level:
    root: info