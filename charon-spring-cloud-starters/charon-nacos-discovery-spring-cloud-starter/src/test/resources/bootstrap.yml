server:
  port: 8100
spring:
  application:
    name: nacos-discovery
  cloud:
    nacos:
      discovery:
        server-addr: ${charon.nacos.server-addr}
        group: 1.0.0
logging:
  level:
    root: info