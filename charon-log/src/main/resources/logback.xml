<?xml version="1.0" encoding="utf-8"?>
<configuration>
    <contextName>logback</contextName>

    <!-- msg Converter -->
    <conversionRule conversionWord="msg"
                    converterClass="io.github.harvies.charon.log.MySampleConverter"/>


    <property name="pattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} %line - %msg %n"/>
    <!--    <property name="pattern-color"-->
    <!--              value="%yellow(%d{yyyy-MM-dd HH:mm:ss.SSS}) [%thread] %highlight(%-5level) %green(%logger{50}) %line - %highlight(%msg) %n"/>-->
    <!--    <property name="LOG_HOME" value="logs"/>-->

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="io.github.harvies.charon.log.MyConsoleAppender">
<!--        <filter class="io.github.harvies.charon.log.CustomerFilter"/>-->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${pattern}</pattern>
        </encoder>
    </appender>

    <!--    &lt;!&ndash; 控制台输出-带颜色 &ndash;&gt;-->
    <!--    <appender name="CONSOLE-WITH-COLOR" class="io.github.harvies.charon.core.utils.MyConsoleAppender">-->
    <!--        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">-->
    <!--            <pattern>${pattern-color}</pattern>-->
    <!--        </encoder>-->
    <!--    </appender>-->

    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/all.%d.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${pattern}</pattern>
        </encoder>
    </appender>


    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="io.github.harvies.charon" level="info" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

</configuration>
